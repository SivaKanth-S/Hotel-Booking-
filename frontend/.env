# Development: Leave blank or unset so api.js falls back to relative '/api'
# which is proxied by Vite to http://localhost:8080 (avoids CORS issues in dev).
# Production: Set to your deployed backend URL e.g. https://api.yoursite.com/api
# VITE_API_BASE_URL=https://api.yoursite.com/api
